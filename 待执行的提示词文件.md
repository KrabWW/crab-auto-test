# 待执行的提示词文件

> 本文件记录待执行的 OMC 提示词。需要执行时打开本文件复制对应提示词运行。
> 本条为 **Phase 3 spec-first 门控** 提示词：补齐 Phase 3 的 OpenSpec spec（不写业务代码）。
> Phase 3 实现须待本轮 spec 批准 + 归档后，另起实现 autopilot（见文末"下一步"）。

---

## Phase 3 spec 补齐 autopilot（待执行）

```text
/oh-my-claudecode:autopilot

补齐 crab-auto-test Phase 3 的 OpenSpec spec（spec-first 门控，不写业务代码）。基于已合并 main 的
6d09435（MVP + Phase 2 已完成）。读 crab-auto-test/openspec/changes/rebuild-ai-test-platform/PLAN-SPECFILL.md
与 PLAN-UNIFIED.md（§4 Phase 3 + §11 非实现清单）为权威。

背景：Phase 3 的 5 个 capability（test-suite / api-automation 完整套件 / requirement-management /
llm-chat / mcp-admin UI）在当前 OpenSpec 下非约束性，须先补 spec 才能实现（§11 d1 spec-first 门控）。
本任务即门控本身——只写 OpenSpec spec/proposal/design/tasks，不实现 cap 业务代码。

载体 V-c（PLAN-SPECFILL §4.2）：
1. 第二 change `openspec/changes/fill-spec-gaps-phase3/`（纯 ADDED，零 MODIFIED，可立即 openspec validate）：
   5 个新 cap spec，每个 2-4 个 Requirement + 可测 WHEN/THEN Scenario；自身 proposal.md / design.md / tasks.md。
   - test-suite：自包含 SuiteRun（持执行记录 ID 列表），不改 test-asset-management 功能 FK（§11 e6）
   - api-automation：显式 Phase-3 门控（MVP 占位路由已存在）；secrets 信封加密引用；无上游 DSL/prompt 复用（§11 e9）
   - requirement-management：draft→reviewed→approved，仅 owner 批准，无新角色（§11 a2）；已批准需求回链生成用例
   - llm-chat：会话级，无 tool-calling/多步/人审，不属 backend-ai-orchestration R2 范围（§11 e8）
   - mcp-admin：MCP 工具限定项目级，无全局工具/全局 admin（§11 a5/e7）；拥有"非白名单拒绝"谓词单一真相源
2. 第三 change `openspec/changes/fill-spec-gaps-modifieds/`（post-archive，所有 MODIFIED deltas，显式枚举 5 项）：
   - web-ui：ADDED R4（chat 路由）/ R5（mcp-admin 路由）/ R6（api-automation 路由，MVP 占位 + Phase 3 完整），不 fattening R3
   - backend-ai-orchestration：MODIFIED R3 仅引用 mcp-admin allowlist（"MCP invocations SHALL use the mcp-admin allowlist"），不重述拒绝谓词
   - ai-test-generation：MODIFIED R1 增"已批准受管需求为可选输入 + 生成用例回链需求版本"场景
   - clean-room-rebuild：ADDED R5（"Non-goal reconciliation is explicit"）——永久非目标保持；API 自动化套件 + 完整 MCP 管理 lift 到 Phase-3 spec-first；保留 R3 原措辞（§11 e5 不重写）
   - test-asset-management：仅性能索引/约束联动（test-suite 自包含 SuiteRun，无功能 FK 改动）

硬约束：
- OpenSpec 唯一需求源；不臆造超 OpenSpec 的 binding 需求（§11 e4）
- 不复制 WHartTest 任何源码/素材/Logo/文案/样式/配置/提示词/布局（clean-room；每新 cap 附 provenance 场景或 design 注记）
- 本轮只写 OpenSpec spec/proposal/design/tasks，不写业务代码、不实现 cap
- V-c：第二 change 纯 ADDED（须能 openspec validate 通过）；第三 change 须 prior change 归档后创建（MODIFIED 解引真实 openspec/specs/）
- 不碰 MVP/Phase 2 已实现代码
- 简单角色不变量（owner/member，无新角色）；不实现企业微信/复杂 RBAC
- 不引入 LangGraph checkpointer（R1）/BullMQ 传输（R2）/重放缓冲（R8）/外部密钥管理器（R5）
- 第三 change 的 clean-room-rebuild 用 ADDED R5，不重写 R3 原措辞（§11 e5）
- test-suite 不经修改 test-asset-management 功能 FK 实现联动（§11 e6，自包含 SuiteRun）
- llm-chat 不触动 backend-ai-orchestration R2 范围（§11 e8，会话级声明）
- mcp-admin 不引入全局 admin 角色/平台全局工具（§11 a5/e7）

验收（PLAN-SPECFILL §6 spec 质量门，二值 pass/fail）：
- Gate A 场景可测性：每 scenario ≥1 WHEN + ≥1 THEN，THEN 二值可检，无 "should/may/appropriately" 含糊
- Gate B clean-room provenance：每新 cap 有 clean-room 场景或 design provenance 注记；无上游 DSL/prompt/asset/layout 引用
- Gate C 非目标 diff：永久非目标（企业微信/复杂 RBAC/全 DOCX 编辑器/分布式 UI 执行/全局 MCP 工具）仍拒；API 自动化套件 + 完整 MCP 管理 lift 到 Phase-3 spec-gated；无其他非目标被暗中 lift
- Gate D 跨 cap 一致性：耦合单向（mcp-admin→backend-ai-orchestration、requirement-management→ai-test-generation、test-suite→test-asset-management 自包含、llm-chat→platform-foundation、web-ui→3 新 UI cap），被引 requirement 存在，无悬空引用；simple-roles 无蔓延
- 5 新 cap spec 文件齐全（第二 change）+ 第三 change proposal/design/tasks + 5 项 MODIFIED/ADDED 枚举
- 第二 change `openspec validate` 通过

每完成一个 change 提交一次（docs(crab-auto-test): Phase 3 spec - <change>），全部完成后合并到 main。
注意：本轮不实现 cap 业务代码；实现是 spec 批准+归档后的独立 follow-on autopilot。
```

---

## 执行前检查

- 当前基线：`main` 已合并 `6d09435`（MVP + Phase 2 完成）
- 权威文档：`openspec/changes/rebuild-ai-test-platform/PLAN-SPECFILL.md`（5 cap spec 结构 + V-c 载体 + 4 质量门）、`PLAN-UNIFIED.md` §4/§11
- 前置：Phase 2 已合并（KB/RAG + Skills + MCP 机制已在 main）；prior change `rebuild-ai-test-platform` 须先归档（`openspec archive`）才能创建第三 change 的 MODIFIED
- 执行方式：复制上方 ```text``` 块内全部内容（含 `/oh-my-claudecode:autopilot` 首行）粘贴到 Claude Code 运行

## 下一步（spec 批准后）

Phase 3 spec 补齐并归档后，5 cap 实现是独立 autopilot（每个 cap 一个，按 test-suite → api-automation → requirement-management → llm-chat → mcp-admin 顺序，spec 批准后方可开工）。实现提示词待 spec 归档后另写。
